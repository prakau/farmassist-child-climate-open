services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes: ["farmassist-data:/app/data"]
    environment: ["DATABASE_URL=sqlite:////app/data/farmassist.db"]
  dashboard:
    build: apps/dashboard
    ports: ["5173:80"]
    depends_on: [api]
volumes:
  farmassist-data:
